import {RoundingPipe} from './rounding-pipe.pipe';

describe('RoundingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundingPipe();
    expect(pipe).toBeTruthy();
  });
});
