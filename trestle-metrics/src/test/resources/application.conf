trestle {
  metrics {
    backend {
      class: "com.nickrobison.trestle.metrics.backends.PostgresBackend"
      connectionString: "jdbc:postgresql://localhost:5432/trestle?currentSchema=metrics"
      username: "nrobison"
      password: ""
      schema: "metrics"
    }
  }
}